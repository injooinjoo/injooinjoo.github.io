import React from 'react';
import ReactDOM from 'react-dom/client';
import SidekickLanding from './SidekickLanding';

ReactDOM.createRoot(document.getElementById('sidekick-root')!).render(
  <React.StrictMode>
    <SidekickLanding />
  </React.StrictMode>,
);
